[project]
name = "license-to-act"
version = "0.1.0"
description = "Prototype authority layer for state-changing agents."
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
